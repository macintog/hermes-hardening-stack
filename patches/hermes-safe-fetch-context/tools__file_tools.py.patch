diff --git a/tools/file_tools.py b/tools/file_tools.py
index bdc1bc8cf..e3a0ff7e3 100644
--- a/tools/file_tools.py
+++ b/tools/file_tools.py
@@ -565,6 +565,11 @@ _SENSITIVE_PATH_PREFIXES = (
 )
 _SENSITIVE_EXACT_PATHS = {"/var/run/docker.sock", "/run/docker.sock"}
 
+
+def _is_macos_per_user_temp_path(path: str) -> bool:
+    return os.path.normpath(path).startswith("/private/var/folders/")
+
+
 _hermes_config_resolved: str | None = None
 _hermes_config_resolved_loaded = False
 
@@ -597,6 +602,8 @@ def _check_sensitive_path(filepath: str, task_id: str = "default") -> str | None
         "Use the terminal tool with sudo if you need to modify system files."
     )
     for prefix in _SENSITIVE_PATH_PREFIXES:
+        if prefix == "/private/var/" and _is_macos_per_user_temp_path(resolved):
+            continue
         if resolved.startswith(prefix) or normalized.startswith(prefix):
             return _err
     if resolved in _SENSITIVE_EXACT_PATHS or normalized in _SENSITIVE_EXACT_PATHS:
