diff --git a/model_tools.py b/model_tools.py
index 539b0e13..3c21d984 100644
--- a/model_tools.py
+++ b/model_tools.py
@@ -27,6 +27,8 @@ import threading
 import time
 from typing import Dict, Any, List, Optional, Tuple
 
+from agent.action_authority import AuthorityDecision, evaluate_action_authority
+from agent.context_safety import render_model_visible_tool_result
 from tools.registry import discover_builtin_tools, registry
 from toolsets import resolve_toolset, validate_toolset
 
@@ -500,6 +502,7 @@ def handle_function_call(
     user_task: Optional[str] = None,
     enabled_tools: Optional[List[str]] = None,
     skip_pre_tool_call_hook: bool = False,
+    prior_untrusted_context: Optional[str] = None,
 ) -> str:
     """
     Main function call dispatcher that routes calls to the tool registry.
@@ -513,6 +516,8 @@ def handle_function_call(
                        execute_code uses this list to determine which sandbox
                        tools to generate.  Falls back to the process-global
                        ``_last_resolved_tool_names`` for backward compat.
+        prior_untrusted_context: Model-visible prior tool/evidence text that
+                       must not authorize this tool call.
 
     Returns:
         Function result as a JSON string.
@@ -521,6 +526,21 @@ def handle_function_call(
     function_args = coerce_tool_args(function_name, function_args)
 
     try:
+        authority = evaluate_action_authority(
+            function_name,
+            function_args,
+            trusted_user_intent=user_task or "",
+            # Agent-loop tools are mediated by run_agent.py, but classify them
+            # here too so direct calls cannot bypass the deterministic gate.
+            policy_allows_without_user_intent=function_name in _AGENT_LOOP_TOOLS,
+            prior_untrusted_context=prior_untrusted_context
+            or (user_task if user_task and "<untrusted-context" in user_task else None),
+        )
+        if authority.decision == AuthorityDecision.BLOCK:
+            return json.dumps(authority.to_error_payload(), ensure_ascii=False)
+        if authority.decision == AuthorityDecision.REQUIRE_CONFIRMATION:
+            return json.dumps(authority.to_error_payload(), ensure_ascii=False)
+
         if function_name in _AGENT_LOOP_TOOLS:
             return json.dumps({"error": f"{function_name} must be handled by the agent loop"})
 
@@ -633,6 +653,7 @@ def handle_function_call(
         except Exception:
             pass
 
+        result = render_model_visible_tool_result(function_name, result, source_label=function_name)
         return result
 
     except Exception as e:
